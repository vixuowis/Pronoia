#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")" && pwd)"
PY="$ROOT/backend/.venv/bin/python"

if [ ! -x "$PY" ]; then
  PY="${PYTHON:-python3}"
fi

cd "$ROOT/backend"
exec "$PY" -m app.cli "$@"
